import { TradesPanel } from "@/components/simulator/trades-panel";

export const metadata = {
  title: "Giao dịch - JokingFinance",
};

export default function AppTradesPage() {
  return <TradesPanel />;
}
