import { JournalPanel } from "@/components/simulator/journal-panel";

export const metadata = {
  title: "Nhật ký - JokingFinance",
};

export default function AppJournalPage() {
  return <JournalPanel />;
}
