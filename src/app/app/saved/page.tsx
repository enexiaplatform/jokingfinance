import { SavedContentPanel } from "@/components/app/saved-content-panel";

export const metadata = {
  title: "Danh sách học đã lưu - JokingFinance",
};

export default function SavedContentPage() {
  return <SavedContentPanel />;
}
