import { ToolsPanel } from "@/components/simulator/tools-panel";

export const metadata = {
  title: "Hộp công cụ tài chính - JokingFinance",
  description: "Bộ công cụ tự kiểm định tâm lý trước giao dịch và mô phỏng chi phí cơ hội, lãi kép kỷ luật.",
};

export default function AppToolsPage() {
  return <ToolsPanel />;
}
