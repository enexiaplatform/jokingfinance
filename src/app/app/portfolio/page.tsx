import { PortfolioPanel } from "@/components/simulator/portfolio-panel";

export const metadata = {
  title: "Danh mục - JokingFinance",
};

export default function AppPortfolioPage() {
  return <PortfolioPanel />;
}
